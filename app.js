//1. necesitamos el elemento de entrada, en este caso es el numero de tarjeta

do {
  var creditCard = parseInt(prompt("Ingrese el numero de tarjeta de credito");
}   while (!creditCard) ;


function isValidCard (creditCard) {
     var numArr = creditCard.split("");
}

