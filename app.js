// que necesita mi codigo para inicializar? una entrada

do {
  var creditCard = prompt("Ingrese el numero de tarjeta de credito");
}   while (!creditCard) ;


function isValidCard (creditCard) {
     var numArr = creditCard.split("");
}
